<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="right" >


    <ImageView
        android:id="@+id/shoppingCart"
        android:layout_width="43dp"
        android:layout_height="43dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/cart" />

    <ImageView
        android:id="@+id/quantityBadge"
        android:layout_width="15dp"
        android:layout_height="15dp"
        android:layout_marginStart="24dp"
        android:clickable="true"
        android:src="@drawable/bkg_actionbar_notify_off"
        app:layout_constraintBottom_toBottomOf="@+id/shoppingCart"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/shoppingCart"
        app:layout_constraintVertical_bias="0.107" />

    <TextView
        android:id="@+id/itemQuantity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:text="99"
        android:textSize="8sp"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="@+id/quantityBadge"
        app:layout_constraintEnd_toEndOf="@+id/quantityBadge"
        app:layout_constraintStart_toStartOf="@+id/quantityBadge"
        app:layout_constraintTop_toTopOf="@+id/quantityBadge" />

</androidx.constraintlayout.widget.ConstraintLayout>